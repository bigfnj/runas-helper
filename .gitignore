# Build artefacts
bin/
obj/
publish/

# Visual Studio
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates
UpgradeLog*.htm
UpgradeLog*.xml

# WiX intermediate
*.wixobj
*.wixpdb
*.wixmsi

# NuGet
packages/
*.nupkg
.nuget/

# Code-signing private keys — NEVER commit. The public .cer (signing/*.cer) is
# intentionally tracked; the private key stays in the user's certificate store.
*.pfx
*.p12
*.snk

# Rider / JetBrains
.idea/
*.DotSettings.user

# macOS / Windows noise
.DS_Store
Thumbs.db
*:Zone.Identifier

CLAUDE.md
AGENTS.md
.codex/
.cursorrules
.windsurfrules
SESSION_*

# Maintainer's working journal. Deliberately untracked: it is a dev log of
# machine-specific state and dead ends, not documentation. Keep your own copy
# backed up somewhere, because the repo no longer does it for you.
PLAN.md